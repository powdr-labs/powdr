namespace Global(%N);
    pol constant L1;    // 1, 0, 0, 0, 0,
    pol constant LLAST; // 0,0,0,0,0, ...., 0, 1
    pol constant BYTE;
    pol constant BYTE_2A;
    pol constant BYTE2;
    pol constant CLK32[32];       // [0] = 1, 0:31  (cyclic)
                                  // [1] = 0, 1, 0:30 (cyclic)
                                  // [7] = 0:31, 1 (cyclic)

    pol constant BYTE_FACTOR[8];  // [0] = 1,256,256**2,256**3, 0:28 (cyclic)
                                  // [1] = 0:4, 1,256,256**2,256**3, 0:24 (cyclic)
                                  // [7] = 0:28, 1,256,256**2,256**3 (cyclic)

    /////// Constant Polynomials
    pol constant STEP;  // 0, 1, 2, 3, ...... N-1

    // STEP32 = 0,1,2,...,30,31,0,1,2,...
    pol constant STEP32; // [0..32]

