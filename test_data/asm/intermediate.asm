machine Intermediate with
    latch: latch,
    operation_id: operation_id
{
    col fixed latch = [1]*;
    col fixed operation_id = [0]*;
    col witness x;
    col intermediate = x;
    intermediate = intermediate;
}
