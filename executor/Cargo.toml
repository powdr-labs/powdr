[package]
name = "executor"
version = "0.1.0"
edition = "2021"

[dependencies]
itertools = "^0.10"
log = "0.4.17"
number = { path = "../number" }
parser_util = { path = "../parser_util" }
parser = { path = "../parser" }
pil_analyzer = { path = "../pil_analyzer" }
deterministic_collections = { path = "../deterministic_collections" }
rayon = "1.7.0"

[dev-dependencies]
mktemp = "0.5.0"
