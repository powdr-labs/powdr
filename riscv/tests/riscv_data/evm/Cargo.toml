[package]
name = "evm"
version = "0.1.0"
edition = "2021"

[dependencies]
revm = { git = "https://github.com/bluealloy/revm.git", default-features = false }
runtime = { path = "../../../runtime" }

[workspace]
