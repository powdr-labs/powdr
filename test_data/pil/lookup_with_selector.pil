let N: int = 4;

namespace main(N);
    col witness w;
    col fixed s_w = [1, 0]*;

    // t is essentially {2, 4}
    col fixed t = [1, 2, 3, 4]*;
    col fixed s_t = [0, 1]*;

    s_w $ [w] in s_t $ [t];
